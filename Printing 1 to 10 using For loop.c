#include <stdio.h>

int main()
{
    int a;
    //using for loop to iterate from 1 to 10
    for(int i=1;i<=10;i++){
        a=i; //assigning the current value of i to a
        //printing the value of a which is also the value of i
        printf("%d\n",a);
    }
    return 0;
}
